👋 Update my TrueView codebase (pre-crisis version) with all of the following cost-protection and scalability fixes.

⚠️ NOTE: This version also introduces a photo caching system. During enrichment, download and store Google Place photos as static assets — not just temporary Google URLs.

---

✅ [1] Fix `enrichAllCommunities()` in `server/comprehensive-photo-enrichment.ts`

• Add a `MAX_PHOTOS_PER_COMMUNITY` constant (set to 5)
• Limit the number of Google photo references using `.slice(0, MAX_PHOTOS_PER_COMMUNITY)`
• For each photo:
   → Call Google Place Photo API with the `photo_reference`
   → Download the actual image (as a buffer or stream)
   → Upload that image to a permanent location:
      - Firebase Storage, S3, or local `/public/photos/`
   → Generate a permanent URL like `/photos/{communityId}-{index}.jpg`
   → Save this permanent URL in the `photos[]` field of the community record
• Avoid storing raw Google-generated photo URLs in production

• Add a global "enrichment lock" (e.g. DB flag or memory cache) to prevent concurrent runs
• Use `ApiCostProtection.checkBeforeOperation()` before every loop
• Add a 5-second delay between each community’s enrichment cycle

---

✅ [2] Fix `google-places-integration.ts`

• If `checkBeforeOperation()` returns `allowed: false`, throw a hard stop (don’t return null)
• Prevent automatic retries — errors must be resolved manually
• Add basic circuit breaker:
   - Track API failures over a sliding 5-minute window
   - If more than 3 consecutive failures occur, temporarily trip the breaker

---

✅ [3] Fix `routes.ts`

• Enforce enrichment lock for all routes like `/api/enrich-all`
• Block requests if another enrichment is running
• Add rate limit: max 1 enrichment trigger per 10 seconds per IP/user
• Check `EmergencyApiDisable.checkApiAccess()` at the top of each route

---

✅ [4] Frontend Fixes

• `admin.tsx`, `expansion-monitor.tsx`, and `api-cost-dashboard.tsx`:
   → REMOVE all `refetchInterval` values from `useQuery()`
   → Replace with manual refresh buttons only

• In `queryClient.ts`, set `retry: false` globally or at least for cost/reporting queries

• In `search.tsx` and `home.tsx`:
   → Use only locally stored photos (from the DB)
   → Never ping Google for images in real time

---

✅ [5] Database Schema

• Ensure `photos` is a string array (JSONB)
• Add `enrichmentCompleted: boolean` and `lastEnrichedAt: Date`
• Skip enrichment if `enrichmentCompleted` is true and `lastEnrichedAt` is recent

---

✅ [6] Photo Caching Integration (new)

• Create a helper:
```ts
async function downloadAndCacheGooglePhoto(photoReference: string, communityId: string, index: number): Promise<string> {
  // Call Google Place Photo API
  // Download image (axios or fetch with stream/arrayBuffer)
  // Upload image to /public/photos/ or cloud
  // Return permanent URL like `/photos/{communityId}-{index}.jpg`
}