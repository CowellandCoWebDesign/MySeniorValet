Got it — here's the full message to copy-paste into Ghostwriter (or any dev assistant working on your TrueView app):


---

🚀 INSTRUCTION: CDSS Facility Scraper for Initial Data Seeding

We need to pull RCFE facility data from the California Department of Social Services website to seed our senior housing app database. Below is a Python script that:

Sends a search POST request to CDSS

Parses basic facility details from the HTML

Outputs to a CSV for now (can be redirected to a DB later)



---

🔧 TASKS FOR YOU (Ghostwriter):

1. Run this script as-is to verify we can get results for Shasta County


2. Once verified, loop it through all CA counties to build the full dataset


3. Eventually send data to our database instead of CSV


4. Flag any anti-bot issues (we’ll proxy or rotate if needed)




---

✅ Copy this into a file named cdss_scraper.py

import requests
from bs4 import BeautifulSoup
import csv
import time

# Configuration
BASE_URL = "https://www.ccld.dss.ca.gov/carefacilitysearch/Results"
HEADERS = {
    'User-Agent': 'Mozilla/5.0 (compatible; TrueViewBot/1.0; +https://trueviewsenior.com)'
}
COUNTY = "Shasta"
FACILITY_TYPE = "Residential Care Elderly"

def scrape_cdss(county=COUNTY, facility_type=FACILITY_TYPE):
    payload = {
        'facilityType': facility_type,
        'county': county,
        'facilityName': '',
        'city': '',
        'zip': '',
        'licNum': '',
        'action': 'Search'
    }

    response = requests.post(BASE_URL, data=payload, headers=HEADERS)
    soup = BeautifulSoup(response.text, 'html.parser')
    cards = soup.find_all('div', class_='result-card')

    results = []
    for card in cards:
        name = card.find('h4').text.strip() if card.find('h4') else ''
        details = card.find_all('p')
        data = {
            'Name': name,
            'Address': '',
            'Phone': '',
            'License Number': '',
            'Facility Type': '',
            'Capacity': '',
            'Status': '',
            'County': county
        }

        for detail in details:
            text = detail.text.strip()
            if "Address:" in text:
                data['Address'] = text.replace("Address:", "").strip()
            elif "Phone:" in text:
                data['Phone'] = text.replace("Phone:", "").strip()
            elif "License Number:" in text:
                data['License Number'] = text.replace("License Number:", "").strip()
            elif "Facility Type:" in text:
                data['Facility Type'] = text.replace("Facility Type:", "").strip()
            elif "Capacity:" in text:
                data['Capacity'] = text.replace("Capacity:", "").strip()
            elif "Status:" in text:
                data['Status'] = text.replace("Status:", "").strip()

        results.append(data)

    return results

def save_to_csv(data, filename="cdss_facilities.csv"):
    keys = data[0].keys()
    with open(filename, "w", newline='', encoding='utf-8') as f:
        writer = csv.DictWriter(f, keys)
        writer.writeheader()
        writer.writerows(data)

if __name__ == "__main__":
    print(f"Scraping CDSS for {COUNTY} County ({FACILITY_TYPE})...")
    results = scrape_cdss()
    if results:
        save_to_csv(results)
        print(f"Saved {len(results)} facilities to cdss_facilities.csv")
    else:
        print("No results found.")


---

Let me know if you want the version that loops through all counties, pushes to PostgreSQL, or includes pagination and report PDF downloads.

