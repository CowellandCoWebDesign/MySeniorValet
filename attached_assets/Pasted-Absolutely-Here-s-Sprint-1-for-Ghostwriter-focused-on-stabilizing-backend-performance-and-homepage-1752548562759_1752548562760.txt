Absolutely. Here's Sprint 1 for Ghostwriter, focused on stabilizing backend performance and homepage responsiveness.


---

🏁 Sprint 1 – Backend & Homepage Stabilization (3 Days)

🎯 Goal:

Resolve the most severe performance bottlenecks on search and homepage load by optimizing the database, enabling caching, and consolidating API calls.


---

✅ Day 1 – Critical Database Fixes

Tasks:

[ ] Add the following indexes:

CREATE INDEX CONCURRENTLY idx_communities_city ON communities USING btree(city);
CREATE INDEX CONCURRENTLY idx_communities_state ON communities USING btree(state);
CREATE INDEX CONCURRENTLY idx_communities_zip_code ON communities USING btree(zip_code);
CREATE INDEX CONCURRENTLY idx_communities_care_types ON communities USING gin(care_types);
CREATE INDEX CONCURRENTLY idx_communities_location_composite ON communities(state, city, zip_code);
CREATE INDEX CONCURRENTLY idx_communities_coordinates ON communities(latitude, longitude) WHERE latitude IS NOT NULL;
CREATE INDEX CONCURRENTLY idx_communities_rating ON communities(google_rating) WHERE google_rating IS NOT NULL;

[ ] Add trending_score column:

ALTER TABLE communities ADD COLUMN trending_score DECIMAL(5,2) DEFAULT 0;
CREATE INDEX idx_communities_trending_score ON communities(trending_score DESC);

[ ] Add updateTrendingScores() background job:

async function updateTrendingScores() {
  await db.execute(sql`
    UPDATE communities 
    SET trending_score = (COALESCE(google_rating, 3.5) * COALESCE(google_review_count, 1))
    WHERE latitude IS NOT NULL AND longitude IS NOT NULL
  `);
}



---

✅ Day 2 – Redis Query Caching

Tasks:

[ ] Initialize Redis client:

import Redis from 'ioredis';
const redis = new Redis(process.env.REDIS_URL);

[ ] Implement caching for trending:

async function getCachedTrendingCommunities(limit = 8) {
  const cacheKey = `trending_communities_${limit}`;
  const cached = await redis.get(cacheKey);
  if (cached) return JSON.parse(cached);

  const data = await db.select()...;
  await redis.setex(cacheKey, 300, JSON.stringify(data));
  return data;
}

[ ] Repeat similar caching logic for:

getCachedLocationCommunities(city, limit)

getCachedCoastalCommunities(limit)

communityStatsCache.getTotalCount()


[ ] Log cache hits/misses for monitoring:

const cacheStats = {
  hits: 0,
  misses: 0,
  hitRate: function () {
    return this.hits / (this.hits + this.misses) * 100;
  }
};



---

✅ Day 3 – Homepage API Consolidation

Tasks:

[ ] Create GET /api/homepage/data endpoint:

app.get('/api/homepage/data', async (req, res) => {
  const [
    communityCount,
    heroImages,
    trending,
    sacramento,
    coastal,
    california
  ] = await Promise.all([
    communityStatsCache.getTotalCount(),
    getHeroImages(),
    getCachedTrendingCommunities(8),
    getCachedLocationCommunities('Sacramento', 20),
    getCachedCoastalCommunities(20),
    getCachedLocationCommunities('California', 20)
  ]);
  res.json({ communityCount, heroImages, trending, sacramento, coastal, california });
});

[ ] Update frontend:

const { data, isLoading } = useQuery({
  queryKey: ['/api/homepage/data'],
  queryFn: () => fetch('/api/homepage/data').then(res => res.json()),
  staleTime: 300_000 // 5 minutes
});



---

🧪 Testing Checklist

[ ] Verify DB indexes exist using pg_indexes

[ ] Confirm trending_score values populated

[ ] Test Redis cache population (watch keys, TTLs)

[ ] Validate new API endpoint returns all necessary data

[ ] Confirm homepage loads with single API call

[ ] Measure load time improvement in DevTools



---

📊 Expected Results (Post-Sprint 1)

Metric	Before	After Sprint 1

Homepage API load	2–5s	200–500ms
Trending communities	200–500ms	10–50ms
Query caching hit rate	0%	80–90%
Backend CPU usage	High	Moderate



---

When you’re ready, just say “Sprint 2”, and I’ll lay out the next steps for search logic and query simplification.

