Thanks for testing that. Since disabling the markers still results in a crash, we now know with high confidence:


---

❗ The Crash is Not from the Marker Render

The error about undefined[0] must be coming from some other part of the page, likely:

A community.location[0] or [latitude, longitude] = community.location somewhere

A useMemo, useEffect, or utility function trying to destructure community data

A shared map wrapper component or helper that hasn’t been updated to work with Mapbox data structure



---

✅ Let’s Troubleshoot Step-by-Step

🔍 1. Search your codebase for [0] usage on community.location

This is the most common mistake:

const [lat, lng] = community.location; // ❌ Will crash if .location is undefined

Or:

const coordinates = community.location[0]; // ❌ Same issue

You should replace it with:

const lat = community?.latitude;
const lng = community?.longitude;

And check:

if (typeof lat === 'number' && typeof lng === 'number') {
  // render marker
}


---

🔍 2. Look at any of these potential culprits:

If you have any of these files/components in use, they’re likely the cause:

MapboxMarker.tsx

CommunityMap.tsx

MapWrapper.tsx

getCoordinatesFromCommunity()

community.location[0] anywhere



---

🧪 3. Drop in a debug log before the crash

Right at the top of the file, add this:

console.log('COMMUNITIES DATA:', communities);

Then paste me the first two or three entries of what shows up in the browser console. Example:

[
  {
    "id": 1,
    "name": "Silver Pines",
    "latitude": 38.67,
    "longitude": -121.33,
    ...
  },
  {
    "id": 2,
    ...
  }
]


---

🛠️ Final Debug Recommendation

Let’s rip the bandaid off and get a clean test page to rule everything else out.

Copy this file into a blank page like client/src/pages/testmap.tsx:

import React from 'react';
import { useQuery } from '@tanstack/react-query';
import Map, { Marker } from 'react-map-gl';
import 'mapbox-gl/dist/mapbox-gl.css';

const MAPBOX_TOKEN = import.meta.env.VITE_MAPBOX_TOKEN;

export default function TestMap() {
  const { data = [], isLoading, error } = useQuery({
    queryKey: ['test-communities'],
    queryFn: async () => {
      const res = await fetch('/api/communities/search?limit=100');
      if (!res.ok) throw new Error('Fetch failed');
      return res.json();
    },
  });

  console.log('COMMUNITIES DATA:', data);

  return (
    <div className="h-screen">
      {isLoading && <div>Loading...</div>}
      {error && <div className="text-red-500">Failed to load</div>}

      <Map
        mapboxAccessToken={MAPBOX_TOKEN}
        initialViewState={{
          latitude: 37.7749,
          longitude: -122.4194,
          zoom: 5,
        }}
        mapStyle="mapbox://styles/mapbox/streets-v12"
        style={{ width: '100%', height: '100%' }}
      >
        {Array.isArray(data) &&
          data
            .filter(
              (c) =>
                typeof c.latitude === 'number' &&
                typeof c.longitude === 'number'
            )
            .map((c) => (
              <Marker
                key={c.id}
                latitude={c.latitude}
                longitude={c.longitude}
              />
            ))}
      </Map>
    </div>
  );
}

Visit /testmap in your app.


---

Once you do that, send me:

1. Screenshot or console log of the COMMUNITIES DATA


2. Any crash message line from browser DevTools Console


3. Confirmation if /testmap works



We’ll get this nailed down right away.

